.row
  .col-md-12
    .page-header
      %h1
        = @user.name
      = form_for [:admin, @user] do |f|
        = render partial: 'users/form', locals: { f: f }
        %p.text-right
          = f.submit nil, class: 'btn btn-success'